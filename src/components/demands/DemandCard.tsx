// DemandCard is no longer used — Demandas now uses a table layout.
// Kept as empty export to avoid import errors if referenced elsewhere.
export default function DemandCard(_props: any) {
  return null;
}
